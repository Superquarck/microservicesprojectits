version: '3'

services:
  
  database: 
    build: .\C:\Users\feder\Desktop\MicroservicesProjectITS\ClientiDB
  enviromet:
    POSTGRES_DB: UserDB
    POSTGRES_USER: fede
    POSTGRES_PASSWORD: mypassword

networks:
  bridge: 

